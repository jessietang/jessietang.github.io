# 个人博客

这是一个使用 Jekyll 构建的个人博客项目，部署在 GitHub Pages 上。

## 功能特性

- ✨ 简洁美观的设计
- 📱 响应式布局，支持各种设备
- 📝 使用 Markdown 编写文章
- 📦 自动部署到 GitHub Pages
- 🏷️ 文章分类功能
- 🔍 文章搜索支持
- 📱 社交链接集成
- 🌐 支持自定义域名

## 技术栈

- **Jekyll**：静态网站生成器
- **GitHub Pages**：网站部署平台
- **Markdown**：文章编写格式
- **CSS**：样式设计
- **HTML**：页面结构

## 本地开发

### 前提条件

- Ruby 2.7.0 或更高版本
- Bundler

### 安装步骤

1. 克隆仓库到本地

```bash
git clone https://github.com/your-username/your-username.github.io.git
cd your-username.github.io
```

2. 安装依赖

```bash
bundle install
```

3. 启动本地服务器

```bash
bundle exec jekyll serve
```

4. 在浏览器中访问 `http://localhost:4000`

## 编写文章

### 创建新文章

1. 在 `_posts` 目录下创建一个新的 Markdown 文件
2. 文件命名格式：`年-月-日-标题.md`，例如：`2026-01-08-hello-world.md`

### 文章格式

每篇文章的顶部需要添加 YAML 前置元数据，例如：

```yaml
---
layout: post
title: 文章标题
date: 2026-01-08
categories: [分类1, 分类2]
---
```

### 文章内容

使用 Markdown 编写文章内容。支持的功能包括：

- 标题
- 段落
- 列表（有序和无序）
- 链接
- 图片
- 代码块
- 引用
- 表格
- 任务列表

### 摘要功能

在文章中添加 `<!-- more -->` 分隔线，分隔线之前的内容会显示在首页的文章列表中，点击"阅读更多"后可以查看完整内容。

## 项目结构

```
.
├── _config.yml          # Jekyll 配置文件
├── _includes/           # 可复用的页面组件
├── _layouts/            # 页面模板
│   ├── default.html     # 默认模板
│   └── post.html        # 文章模板
├── _posts/              # 博客文章
├── _site/               # 生成的静态网站（自动生成，无需修改）
├── about.md             # 关于页面
├── categories.md        # 分类页面
├── css/                 # CSS 样式文件
│   └── main.css         # 主样式文件
├── images/              # 图片资源
├── index.html           # 首页
└── README.md            # 项目说明文件
```

## 自定义配置

### 修改网站信息

在 `_config.yml` 文件中修改网站的基本信息：

```yaml
name: "你的博客名称"
description: "博客描述"
author: 你的名字
email: your-email@example.com
url: "https://your-username.github.io"
```

### 修改主题颜色

在 `css/main.css` 文件中修改主题颜色变量：

```css
/* 主色调 */
color: #007bff;
```

### 添加社交链接

在 `_config.yml` 文件中添加或修改社交链接：

```yaml
social:
  github: your-github-username
  weibo: your-weibo-id
  twitter: your-twitter-username
  linkedin: your-linkedin-username
```

## 部署到 GitHub Pages

### 自动部署

1. 将代码推送到 GitHub 仓库的 `main` 分支
2. GitHub Pages 会自动构建和部署你的网站
3. 访问 `https://your-username.github.io` 查看网站

### 自定义域名

1. 在 `_config.yml` 文件中添加：
   ```yaml
   url: "https://your-domain.com"
   baseurl: ""
   ```

2. 在仓库根目录创建一个 `CNAME` 文件，内容为你的域名：
   ```
your-domain.com
   ```

3. 在你的域名注册商处添加 DNS 记录，将域名指向 `your-username.github.io`

## 常见问题

### 如何添加新页面？

1. 在根目录创建一个新的 Markdown 或 HTML 文件
2. 在文件顶部添加 YAML 前置元数据
3. 在 `_config.yml` 的 `header_pages` 中添加该页面

### 如何修改导航菜单？

修改 `_layouts/default.html` 文件中的导航部分：

```html
<nav class="site-nav">
    <ul>
        <li><a href="{{ site.baseurl }}/">首页</a></li>
        <li><a href="{{ site.baseurl }}/blog/">博客</a></li>
        <li><a href="{{ site.baseurl }}/about/">关于</a></li>
        <li><a href="{{ site.baseurl }}/categories/">分类</a></li>
    </ul>
</nav>
```

### 如何添加新的分类？

在文章的 YAML 前置元数据中添加新的分类名称，Jekyll 会自动创建该分类。

## 许可证

MIT License

## 贡献

欢迎提交 Issues 和 Pull Requests！

## 致谢

- [Jekyll](https://jekyllrb.com/)
- [GitHub Pages](https://pages.github.com/)
- [Markdown](https://daringfireball.net/projects/markdown/)

## 联系方式

如有任何问题或建议，欢迎通过以下方式联系我：

- GitHub: [your-github-username](https://github.com/your-github-username)
- Email: your-email@example.com